<%= link_to 'Ideas Blog', root_path %>
|
<%= link_to 'New Idea', new_idea_path %>
|
<% if user_signed_in? %>
  Hello <%= current_user.name %> | <%= link_to 'Logout', session_path, method: :delete, data: { confirm: 'Are you sure you want to logout?' } %>
<% else %>
  <%= link_to 'Login', new_session_path %> | <%= link_to 'Sign Up', new_user_path %>
<% end %>
